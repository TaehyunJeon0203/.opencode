---
name: django-backend
description: Apply safe Django and Django REST Framework patterns for models, views, serializers, auth, validation, transactions, migrations, and tests
compatibility: opencode
metadata:
  category: engineering
---

# django-backend
Inspect models, migrations, serializers/forms, views, URLs, permissions and tests before editing. Validate input, enforce authorization server-side, preserve API contracts, use transactions/constraints when needed and consider existing data during migrations.
