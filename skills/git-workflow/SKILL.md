---
name: git-workflow
description: Use safe Git practices for inspecting diffs, preserving unrelated work, preparing commits, resolving conflicts, and avoiding destructive history changes
compatibility: opencode
metadata:
  category: engineering
---

# git-workflow
Start with `git status`, inspect diffs, preserve unrelated work and avoid destructive history changes. Only commit/push when asked. Re-run relevant verification after conflict resolution or meaningful changes.
